# Project defaults. The primary session schedules independent work in waves.
# Existing user models, permissions and provider settings remain inherited.
[agents]
enabled = true
max_concurrent_threads_per_session = 3
